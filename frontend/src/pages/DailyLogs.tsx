/**
 * OWNER: [E] — Sprint 5
 * Two free-text boxes (workout, meals) + tags + date picker for backfill.
 */
export default function DailyLogs() {
  return <h1 style={{ fontSize: "var(--fs-2xl)" }}>Daily Check-in</h1>;
}
