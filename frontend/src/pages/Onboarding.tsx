/**
 * OWNER: [E] — Sprint 3
 * 4-step wizard: Personal Info / Fitness Goal / Experience / Food Preference. POST /onboard on finish.
 */
export default function Onboarding() {
  return <h1 style={{ fontSize: "var(--fs-2xl)" }}>Onboarding</h1>;
}
