/**
 * OWNER: [D] — Sprint 4
 * Day tabs, exercise list with sets/reps, Start Workout button.
 */
export default function WorkoutDetails() {
  return <h1 style={{ fontSize: "var(--fs-2xl)" }}>Workout</h1>;
}
