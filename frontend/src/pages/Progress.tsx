/**
 * OWNER: [D] — Sprint 5
 * Overview/Workout/Nutrition/Body tabs, calories chart, consistency stats.
 */
export default function Progress() {
  return <h1 style={{ fontSize: "var(--fs-2xl)" }}>Progress</h1>;
}
